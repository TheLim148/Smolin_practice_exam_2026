## Практика 07 — Thread и Runnable

Задание: создать поток через `Thread` и `Runnable`, вывести сообщение из потока.

В проекте используются:

- `MessagePrinter` — класс, реализующий `Runnable`;
- `Thread` — запуск задач в отдельных потоках;
- `Main` — создаёт и запускает два потока.
