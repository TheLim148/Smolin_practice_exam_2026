## Практика 02 — Singleton Logger

Задание: реализовать паттерн Singleton для логирования сообщений.

В проекте используются классы:

- `Logger` — единственный объект логгера;
- `Main` — проверяет, что оба обращения возвращают один и тот же объект.